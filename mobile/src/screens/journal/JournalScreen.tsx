import React from 'react';
import { ScreenPlaceholder } from '../../components/ScreenPlaceholder';

export function JournalScreen(): React.JSX.Element {
  return (
    <ScreenPlaceholder
      title="AI Journal"
      description="Reflect, process entries, and turn raw thoughts into insight."
    />
  );
}
